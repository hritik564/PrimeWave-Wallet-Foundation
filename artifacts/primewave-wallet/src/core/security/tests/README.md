# Security test structure

Phase 0B does not have a test runner or production security implementation.
This folder is reserved for contract tests that must be written alongside the
first concrete implementations.

Do not add tests that merely assert placeholder values. Tests must prove a
security property against a real implementation.