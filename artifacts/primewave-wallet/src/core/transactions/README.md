# Transaction core

Future home for transaction composition, local signing coordination, and
broadcasting boundaries.

Phase 0B adds `contracts.ts` for preview, explicit confirmation, local signing,
and broadcasting interfaces. No signing or broadcasting implementation exists.